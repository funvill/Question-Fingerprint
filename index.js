const qa = require('./qa');

// Debug. Testing. 

var userID = 0; 
console.log( qa.GetQuestion( userID ) ); 

// qa.AnswerQuestion(userID, 3, 0 )
// qa.AddQuestion(0, "Do you like blue cheese?")

console.log( "done" ); 
